# Feature Matrix

## Governing rule
- This matrix is for a macOS paint.net recreation target.
- `paint.net reference baseline` is the authoritative product target.
- GIMP/Krita may inform backend implementation only; they do not redefine UX scope, command flow, or feature naming.

| Area | paint.net reference baseline | FlatPaint target | Phase | Current status |
| --- | --- | --- | --- | --- |
| Workspace shell | Single-window editor, toolbox, toolbar, image surface, history, colors, layers, command surfaces, status bar | Single-window macOS workspace mirroring paint.net workflow with in-window child utility panels, native menu bar, and fixed default palette placement | 1 | Core shell implemented; parity polish still open, and the current Lazarus GUI source now has a baseline prepared-bitmap paint cache for the canvas plus real in-window `Tools` / `Colors` / `History` / `Layers` palettes with direct visibility toggles, close buttons, reset-layout recovery, edge-snap drop behavior, palette-style title bars, drag-state tint feedback, a dedicated top-right utility button cluster, visible top/left rulers, a centered-canvas viewport when the image is smaller than the host, and a segmented bottom status strip with synchronized embedded zoom controls |
| Document tabs | Multiple image tabs with fast document switching and a new-image size prompt | Main-workspace tab strip with a size dialog before creating a new document tab | 4 | Not implemented in the current Lazarus GUI source; the main form still owns one `TImageDocument`, and there is no visible tab strip / image-list control in code yet |
| Command surface parity | Every visible menu item, toolbar icon, utility icon, tool icon, image-list control, and status-bar control is live | Track every visible route in `docs/COMMAND_SURFACE_BASELINE.md`; no placeholder controls allowed | 4-5 | Baseline documented; implementation incomplete, but the current code now closes the `Acquire` and `Save All Images` file-menu gaps in addition to the existing crop, deselect, utility-icon, zoom-chooser, grid/ruler, `Exit`, and `Curves` / `Resize Image` routes; the biggest remaining visible gap is still the missing image-list surface |
| Workspace visual parity | Compact toolbar, compact palette windows, canvas-first composition, bottom status strip, familiar default control placement | Use `docs/UI_PARITY_AUDIT.md` as the binding visual-gap checklist until the default FlatPaint layout reads credibly like paint.net | 4-5 | Audit baseline added; substantial parity work still open, but the current Lazarus shell now has a denser top strip without duplicated tool icons, explicit palette title bars, non-overlapping default palette placement, visible ruler bands, and a more information-dense bottom status strip with adaptive zoom-cluster layout |
| File workflow | New, open, save, save as, import, export, recent files, print | New/open/save/import/export/recent/print implemented in baseline form; deeper parity still open | 4 | Core subset implemented, and the current Lazarus GUI source now keeps a persisted recent-file list through a real `Open Recent` menu plus explicit `Close`, `Print`, and `Exit` command paths, a dedicated `New` image modal, unified "all supported" open/import filters across project and image files, broader raster/project import coverage, safer IO error dialogs, a real settings route for default new-image DPI, a clipboard-first `Acquire...` route, and a live `Save All Images` route that maps to the current single-document shell |
| Undo/redo | Deep history stack covering practical document mutations | Deterministic document-mutation history; non-destructive viewport state stays out of history | 1 | Core subset implemented, and the shared document model now exposes undo/redo depth plus action labels for UI status surfaces |
| Layers | Add, delete, duplicate, rename, reorder, visibility, properties, merge, flatten | Same baseline, with core layer properties already live | 1-2 | Core subset implemented |
| Selection tools | Rectangle, ellipse, lasso, magic wand, move selection, move selected pixels | Same baseline | 2 | Rectangle/ellipse/lasso/wand plus move-selection and move-selected-pixels now exist in the FPC core; the current Lazarus GUI source now also routes the full baseline tool family, and the shared core now supports replace/add/subtract/intersect selection composition, but richer visible tool-option parity still remains |
| Paint tools | Pencil, brush, eraser, fill, gradient, color picker, clone, recolor, zoom, pan | Same baseline | 2 | Partial; the current Lazarus GUI source now routes hard-edged `Pencil`, `Brush`, `Eraser`, `Paint Bucket`, `Gradient`, `Color Picker`, `Zoom`, and `Pan`, but clone/recolor, a dedicated crop tool, and richer paint.net-style per-tool options still remain open |
| Draw tools | Text, line/curve, rectangle, rounded rectangle, ellipse, freeform shape | Same baseline | 3 | Partial; line, rectangle, rounded-rectangle, ellipse, and freeform-shape outline drawing are now present in the FPC core and current Lazarus GUI source, while `Text`, true curve mode, and richer object-style shape options still remain open |
| View controls | Zoom, pan, transparency, grid/rulers, resize handles, fit-on-open behavior for large images | Zoom/pan/transparency in place; grid/rulers, fit-to-view, AppKit-backed pinch-to-zoom, and large-image fit-on-open implemented, resize handles still partial | 2-3 | Core subset implemented, with the current Lazarus GUI source now routing baseline zoom commands, a visible `Zoom` tool, `Zoom to Selection`, a top-toolbar zoom percentage chooser backed by a preset ladder, a bottom-right status-bar zoom slider that now stays in a dedicated right-edge cluster, real `View -> Pixel Grid` and `View -> Rulers` toggles, scale-aware ruler bands, cursor-anchored `Ctrl` / `Command` wheel zoom, centered small-canvas presentation, and unit-aware measurement display in `View` / the status bar; true native pinch and image-list navigation remain open |
| Colors | Primary/secondary colors, alpha-aware editing, palette habits | Same baseline | 2 | Core subset implemented |
| Adjustments | Auto-level, brightness/contrast, curves, levels, hue/saturation, invert, sepia, posterize, B/W | Practical baseline implemented first, advanced parity continues | 3 | Core subset implemented, with auto-level, invert, grayscale, curves, hue/saturation, levels, brightness/contrast, sepia, black/white, and posterize currently present in the code path; `Brightness / Contrast...` now uses a dedicated two-field modal and one combined menu route, `Curves...` now uses a dedicated gamma modal with a slider around the current one-value RGB curve baseline, `Hue / Saturation...` now uses a dedicated two-field modal, `Levels...` now uses a dedicated four-field modal, `Posterize...` now uses a dedicated levels modal with a slider, and richer paint.net-style adjustment dialogs remain open |
| Effects | Blur, noise, distort, photo, render, stylize families | Core built-in subset first, then parity expansion | 3 | Core subset implemented, with blur/sharpen/noise/outline now present in the FPC core and routed in the current Lazarus GUI source; `Blur...` now uses a dedicated radius modal, `Add Noise...` now uses a dedicated amount modal, and the remaining effect-side gaps are now more about breadth and preview fidelity than raw prompt-box collection |
| Resize/canvas ops | Resize, canvas size, rotate, flip, crop | Same baseline | 2 | Core baseline implemented, and the current Lazarus GUI source now exposes the main image-transform commands in menus, including 180-degree rotation plus a dedicated `Resize Image...` modal with aspect lock and nearest-neighbor / bilinear resampling choices |
| Text/rendering | Simple text tool with direct font/size controls | Same baseline adapted through AppKit text rasterization | 3 | Not implemented in the current Lazarus GUI source; there is still no routed text tool or direct font/size surface in code yet |
| Clipboard | Copy, paste, paste as new layer, cut/copy practical flows | Same baseline for current common flows | 4 | Core subset implemented, with the current Lazarus GUI source now routing a more selection-aware in-process clipboard path plus explicit menu routes for selection-only copy and paste-to-new-image flows |
| Menus/Shortcuts | Broad command coverage through menus and shortcuts | Native menu bar and shortcut parity for primary workflows, with explicit Windows-to-macOS translation policy | 1-4 | Core subset implemented, and the current Lazarus top-level menu structure is closer to paint.net now that selection commands are routed through `Edit` instead of a separate `Select` menu, while `View` now exposes direct palette shortcuts on `Command+1...4` |
| Iconography | Distinct tool/panel icons that support fast visual scanning | Cohesive native icon mapping for tools and panel actions | 1-4 | Core subset implemented, with the current Lazarus toolbar now using a curated compact symbol set for the visible tool strip instead of only text labels |
| Tool/Config Options | Tool-sensitive options with visible effect on output | Persistent visible option controls and defaults | 2-4 | Core subset implemented; the current Lazarus GUI source now separates wand tolerance from brush/shape size in the shared option control, and the fuller audited target is now tracked explicitly in `docs/TOOL_OPTIONS_BASELINE.md` |
| Hidden Sheet Options | Save/export dialog options that materially affect output | Explicit save-sheet controls for quality, interlace, flattening, and compatibility messaging | 4 | Core subset implemented |
| Compatibility IO | Practical open/import behavior for foreign content | Flattened import paths for PSD and `.pdn`/`.xcf`/`.kra` until deeper fidelity exists | 4 | Partial; flattened `PSD` import is wired through the shared image loader, and a first-pass flattened `.xcf` import now exists for common 8-bit uncompressed/RLE GIMP files, but `.pdn` and `.kra` are still not implemented while deeper layer-preserving fidelity remains open |
| Plugin system | Windows plugin ecosystem | Deferred | N/A | Deferred |

## Current implemented rows
- Workspace shell: launchable editor shell with in-window child utility panels as the primary tool surfaces, a denser top toolbar-like control strip, a dedicated top-right utility-button cluster, fixed paint.net-style default panel placement on each launch, compact draggable `Tools` / `Colors` / `History` / `Layers` child panels inside the workspace, direct palette visibility toggles, per-palette close buttons, reset-layout recovery, edge-snap drop behavior, explicit palette title bars, drag-state tint feedback, visible top/left rulers, a centered viewport for smaller canvases, a segmented bottom status strip with synchronized zoom controls, canvas preview, direct canvas interaction, menu commands, a real settings dialog route, and a repository-local `.app` bundle
- Document tabs: not yet implemented in the current Lazarus GUI source; there is still one in-memory document surface only, so the tab strip, image-list thumbnails, multi-document switching, and related context actions remain open work
- File workflow: native `.fpd` save/load with bound-path Save and Save As, `Open Recent`, print-panel routing, `Open`-as-document flow for native/raster/compatibility files, explicit import-as-layer flow, and broader raster read support; the new FPC rewrite core now also has its own native layered document format, and the current Lazarus GUI source plus the CLI both route through native-doc open/save/export and layer-management paths, with the Lazarus GUI source now also honoring a real bound-path `Save`, explicit `Save As`, a persisted `Open Recent` list, explicit `Close`, visible `Print` routes in both the menu and toolbar, a dedicated `New` image modal with size/resolution/print calculations, unified "all supported" open/import filters that merge project files with supported image formats, a clipboard-first `Acquire...` route, a current-shell `Save All Images` route, and a real `Exit` command
- Command surface parity: the audit and breakdown are now explicit in `docs/COMMAND_SURFACE_BASELINE.md` and `docs/COMMAND_SURFACE_BREAKDOWN.md`; current code is still short of full one-to-one parity for every visible route, but the current toolbar now exposes crop, deselect, a real utility-icon strip for the core palette/settings/help routes, a zoom percentage chooser, direct grid/ruler view toggles, live `Curves` / `Resize Image` routes, and the `File` menu now includes live `Acquire` and `Save All Images` commands, while the major visible gap still includes the image-list surface
- Workspace visual parity: explicitly tracked in `docs/UI_PARITY_AUDIT.md`; current shell is usable but still too far from paint.net, although the latest pass tightened toolbar density, palette title-bar treatment, and the dark workspace/palette color separation
- File workflow: native `.fpd` save/load with bound-path Save and Save As, `Open`-as-document flow for native/raster/compatibility files, explicit import-as-layer flow, and flat-raster export; broader file-menu parity still remains open
- Undo/redo: mutation-backed deterministic history for document edits, while zoom/pan/grid/ruler state now stays out of history; the shared document model now also keeps human-readable action labels aligned with undo/redo snapshots for the current `History` palette
- Layers: add, delete, duplicate, rename, reorder, visibility, opacity, blend mode, merge down, flatten; the FPC rewrite CLI now also exposes native-document layer duplication, deletion, reordering, renaming, visibility toggles, opacity control, and merge/flatten routes, and the Lazarus GUI source now exposes layer opacity in its current layer controls
- Selection tools: the new FPC rewrite core now has real rectangle/ellipse/lasso selection masks plus contiguous magic-wand selection, move-selection, move-selected-pixels, fill-selection, erase-selection, invert-selection, select-all, deselect, and crop-to-selection behavior; the current Lazarus GUI source also now routes rectangle/ellipse/lasso selection, magic wand, Shift-add, Option-subtract, and Shift+Option intersect combine modes, move-selection, move-selected-pixels, visible selection outlines, and live drag previews, though richer visible tool-option parity and full runtime packaging are still open
- Paint tools: zoom tool, pan tool, paint bucket, linear gradient, sample/pick color, and direct single-tap/multi-stroke hard-edged `Pencil`, `Brush`, and `Eraser` behavior are live; the current Lazarus GUI source now also routes a gradient drag tool, a preset-based zoom chooser, and a zoom click-tool path that uses the same preset ladder with pointer-anchored focal preservation, while clone/recolor/crop-tool parity and fuller visible tool options remain open
- Draw tools: line, rectangle, rounded-rectangle, ellipse, and freeform-shape outline stroke are live; `Text`, true curve mode, and richer object-style shape parity remain open
- View controls: zoom, pan, transparency preview, pixel-grid, rulers, fit-to-view on demand, no-history interactive zoom for continuous input, and initial fit-to-view behavior for very large opened images; the current Lazarus GUI source now also routes `Zoom In`, `Zoom Out`, `Zoom to Selection`, `Actual Size`, `Fit to Window`, `Pixel Grid`, `Rulers`, and unit selection directly in `View`, exposes a visible `Zoom` tool in the compact tool strip, exposes a top-toolbar zoom percentage chooser plus a bottom-right status-bar slider backed by preset steps, keeps the zoom click-tool on that same preset ladder, supports pointer-anchored `Ctrl` / `Command` wheel zoom, renders a zoom-thresholded pixel grid plus scale-aware top/left ruler bands, and reports unit-aware image and cursor measurements through a segmented status strip, while true native pinch and image-list navigation remain open
- Colors: direct primary/secondary color editing, swap, reset, live hex readout in the UI, and a dedicated child colors utility panel; the current Lazarus GUI source now also exposes one-click swap and black/white reset in the toolbar
- Adjustments: auto-level, invert, grayscale, curves, hue/saturation, levels, black/white, sepia, brightness/contrast, and posterize are currently implemented; the new FPC rewrite core now has shared raster paths for that baseline adjustment set, the CLI already exposes `autolevel` / `autoleveldoc`, and the current Lazarus GUI source now routes auto-level, invert, grayscale, `Curves`, `Hue / Saturation`, `Levels`, `Brightness / Contrast`, and `Posterize` directly for the active layer, with `Brightness / Contrast...` now using a dedicated dual-parameter modal on one combined command path, `Curves...` now using a dedicated gamma modal over the current one-value RGB curve implementation, `Hue / Saturation...` now using a dedicated dual-parameter modal, `Levels...` now using a dedicated four-parameter modal, `Posterize...` now using a dedicated levels modal, and fuller multi-point / per-channel curve editing still remaining open
- Effects: blur, sharpen, outline, deterministic noise; the FPC rewrite core now implements that baseline effect family directly in shared code, the CLI exposes raster and native-document routes for it, and the current Lazarus GUI source now has a real `Effects` menu surface for those commands, with both `Blur...` and `Add Noise...` now using dedicated slider-backed modals instead of raw prompts
- Resize/canvas ops: resize canvas, rotate right, rotate left, rotate 180, flip horizontal, flip vertical, crop, crop to selection; the new FPC rewrite core now has baseline crop, nearest-neighbor resize, bilinear resize, flip, 180-degree rotation, and 90-degree rotation support in shared code, and the current Lazarus GUI source now exposes resize image/canvas plus rotate/flip commands directly in `Image`, with `Resize Image...` now using a dedicated aspect-lock modal and explicit resampling selection
- Clipboard: selection-aware copy/cut plus copy-merged and paste image from pasteboard as a new layer; the FPC rewrite core now also has selection extraction, merged-copy, and paste-as-new-layer behavior in shared code, with CLI routes for exporting selected pixels and pasting an external raster into a new native-document layer, and the current Lazarus GUI source now exposes in-process `Cut`/`Copy`/`Copy Selection`/`Copy Merged`/`Paste` routes through that shared core with preserved selection offsets plus explicit `Paste into New Layer` and `Paste into New Image` menu routes
- Menus/Shortcuts: file, edit, image, view, layers, adjustments, effects commands with primary shortcuts, plus documented Windows-to-macOS translation rules; the current Lazarus GUI source now also matches the standard macOS `Command+S`, `Command+Shift+S`, and `Command+X/C/V` baselines in menus, exposes direct palette toggles on `Command+1...4`, and surfaces the main clipboard actions in the toolbar
- Iconography: the current UI no longer relies only on text for tool discoverability; the current Lazarus toolbar now uses a curated compact symbol set in a paint.net-style tool-family order, and the broader icon-language gap remains open beyond that baseline
- Tool/Config Options: brush size, wand tolerance, recolor tolerance, clone size, shape width, text size, gradient orientation, default text, export defaults; the current Lazarus GUI source now switches the shared top option control between brush-size and wand-tolerance semantics based on the active tool
- Tool palette: the visible `Tools` utility surface now defaults to a narrower two-column stack that is materially closer to the tall paint.net tool window shape than the earlier broad four-column card
- Hidden Sheet Options: JPEG quality, PNG interlace, flattening control, compatibility guidance in export/open flows
- Compatibility IO: flattened `PSD` import is available through the shared image loader; flattened `.xcf` import is now also available for common 8-bit uncompressed/RLE GIMP files (normal visibility/opacity composited into one image), while `.pdn` and `.kra` import paths are still explicit open work rather than completed functionality

## Explicit gaps still to close for stronger paint.net parity
- Finer workspace layout fidelity and panel behavior parity
- Broader image-list parity beyond the new baseline tab strip (next/previous navigation, richer context actions, more paint.net-like thumbnail treatment)
- Deeper file-menu parity beyond the new baseline (fuller recent-file management, tighter print workflow polish, and eventual multi-document semantics behind `Save All Images`)
- Full one-to-one implementation and testing of every visible command surface in `docs/COMMAND_SURFACE_BASELINE.md`
- More complete dock/undock behavior for child panels inside the main window
- Drag-time translucency or equivalent anti-obstruction feedback for child-panel movement
- Tighter default palette placement and denser utility-window composition
- Stronger visual density parity for the top toolbar, palettes, and status strip
- Smoother continuous scrolling and general viewport motion polish
- Broader shortcut coverage and more exact menu-command parity
- More advanced adjustment/effect families and edge-case behavior
- Deeper foreign project fidelity beyond the current flattened `PSD` / first-pass `XCF` paths, including still-missing `.pdn` / `.kra` import routes and richer layer-preserving import
- Richer canvas interaction affordances beyond the newly added baseline rulers/grid overlays

## Feature gap policy
- If a paint.net behavior is intentionally deferred, mark it as `Deferred` or describe the exact gap explicitly.
- If a feature is partially implemented, record the missing edge in `docs/PROGRESS_LOG.md`.
- Do not let backend-reference convenience remove or rename a paint.net-facing behavior without documenting the reason.
